# Claude Code プロバイダー固有指示

- WordPress Block API v3 に準拠する
- `esc_html()`, `esc_attr()`, `esc_url()` を適切に使用
- `get_block_wrapper_attributes()` をブロックレンダリングで使用
- テーマ/プラグインのプレフィックスを一貫させる
- 既存コードベースの規約を尊重する
